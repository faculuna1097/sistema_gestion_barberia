# Plan de rediseño — `frontend` (panel de gestión)

Branch de trabajo: `feature/frontend-gestion-ui` (hija de `feature/turnero`).

Este documento es la **bitácora viva** del rediseño visual del panel de gestión.
Acá se anota:
- el plan paso a paso (checkboxes),
- las decisiones tomadas y por qué,
- las consolidaciones pendientes (cosas que quedaron a medio unificar y hay que cerrar al final).

Las **deudas técnicas** detectadas durante la migración ya no viven acá: se movieron a
su propio registro con plan de resolución escalonado → **`docs/deudas_tecnicas_frontend.md`**.

Cuando un paso se completa, marcarlo `[x]`. Cuando aparece algo nuevo, agregarlo
a la sección que corresponda. **No borrar nada hasta que esté resuelto.**

---

## 0. Decisiones tomadas

| # | Decisión | Razón |
|---|---|---|
| D1 | Aplicar el sistema de diseño documentado en `docs/sistema_de_disenio.md`. Densidad **compacta** (admin, desktop/iPad, sesiones largas). | Es la superficie correcta para el público (dueño). |
| D2 | **Inline styles + `theme` como módulo JS** (igual que turnero/barbero), con `useState` para hover. | Consistencia entre fronts. La alternativa híbrida con `:hover` queda en deuda. |
| D3 | **Coexistencia con Tailwind** durante la migración. Se desinstala al final, cuando ya no quede ningún `className=` con utilities. | Evita romper pantallas viejas en cada paso. Transición controlada. |
| D4 | **Consolidar utils** del admin con las del turnero (no copiar y pisar). El admin tiene helpers de dominio (semana ISO, desplazamientos, etc.) que no existen en turnero. | Preservar lógica útil. |
| D5 | El frontend es **horizontal** (desktop/iPad), distinto del turnero/barbero (mobile-first). Los fundamentos de turnero (tokens, primitivos universales) sirven igual; lo que cambia es el layout/shell (Fase 3) y la densidad. | Norte ya definido en sistema de diseño §3.6. |
| D6 | **Loading del admin = spinner, no Skeleton** (excepción consciente a §4.5 del sistema de diseño). Materializada en el primitivo `LoadingState` (Lucide `Loader2` centrado). | Las secciones del admin tienen layouts muy heterogéneos (KPIs, tablas densas, formularios); mantener un Skeleton fiel pantalla por pantalla agrega ruido sin beneficio. El turnero/barbero (vistas uniformes mobile-first) siguen usando Skeleton. |
| D7 | **Fondo del área de contenido del admin = `theme.surfaceAlt`** (no `theme.bg`). Se aplica una sola vez en `PanelAdmin.jsx`; las secciones **no** override su contenedor. | Cards blancas (`theme.surface`) sobre `theme.bg` (#FAFAFA) tienen contraste mínimo y se pierde la jerarquía. `theme.surfaceAlt` (#F4F4F5) da el contraste justo manteniendo el ambiente claro tipo Stripe/Linear. Se descartó la alternativa de `shadowSm` en cards porque el token es imperceptible sobre #FAFAFA (opacity 0.06). |
| D8 | **Variante del primitivo `Tabs` = underline** (border-bottom 2px en `theme.accent` sobre el activo, tabs planos sin contenedor). No segmented pill ni pill individual. Variant prop adicional se difiere hasta que aparezca un segundo patrón visual real (§7.4). | Coincide con el norte Stripe/Clerk del sistema. Sobre `theme.surfaceAlt` (fondo del admin, D7), un contenedor segmentado gris-claro sobre gris-claro queda visualmente sucio — el underline se sostiene sin contenedor. Es el patrón más sobrio y compacto, funciona igual de bien con 2, 3 o 6 tabs. |
| D9 | **Política de divergencia entre fronts**: lo que se mantiene consistente son los **estilos estéticos** (tokens, look & feel). Las **APIs** (props, contenido, estructura interna) pueden divergir legítimamente — no es deuda. Si más adelante el otro front necesita la capacidad, se readapta. Documentada en detalle en `docs/sistema_de_disenio.md` §8. | Tratar como deuda toda divergencia de API inflaba el backlog con tareas que nunca se cierran. La consistencia que importa para que el producto se sienta unificado es **visual**, no estructural. |
| D11 | **`SeccionTurnero` sin `ScreenHeader`** (excepción consciente al patrón del resto del admin). | La sección es densa por naturaleza (agenda barbero × hora cuando se ve "hoy") y la mayor parte de su valor para el dueño está en la verticalidad — sumar el header "Turnero / Vista global de turnos del día" arriba comía ~80px de la grilla sin agregar info que la propia agenda no auto-explique. La identidad visual de la sección se sostiene desde la propia agenda + controles + KPIs. Es la única sección del admin sin `ScreenHeader`. |
| D12 | **Agenda del turnero implementada in-house con CSS grid + position absolute** (sin librería de calendar). Eje vertical = horas (60px por hora), columnas = barberos visibles, cada turno es un `<button>` absolute-positioned dentro de la columna correspondiente, con altura proporcional a la duración real (`fin − inicio`). Línea "ahora" indigo + dot, redibujada cada 60s vía `setInterval`. | Las alternativas evaluadas (`react-big-calendar`, `FullCalendar`) implican ~50–150kb gz y un overhead de overrides agresivos para que respeten tokens del sistema; el caso real es acotado (un día, sin recurrencia, sin drag&drop), la matemática de posicionar bloques en grilla horaria es ~20 líneas, y queda cero dependencia futura que mantener. Si más adelante aparece necesidad de drag&drop / resize / vistas múltiples, se reevalúa con el caso real ya construido. |
| D13 | **`SeccionGestion` SÍ va con `ScreenHeader`** (a diferencia de Turnero, D11). El shell usa `ScreenHeader` ("Gestión") + primitivo `Tabs` underline (D8) en fila propia. Las 6 tabs llevan ícono Lucide. | El usuario manifestó que a futuro evalúa **eliminar o repensar los headers** ("me parecen tontos e inútiles") — anotado como posible cambio transversal, NO decidido. Por ahora Gestión mantiene el header como el resto del admin. Si se decide sacarlos, es un pase global, no una excepción por sección. |
| D14 | **Densidad de fila distinta entre tablas de reporte y tablas CRUD táctiles.** Las tablas de reporte (Balances, Caja, Ventas, Gastos, Planillas) mantienen filas compactas con separador `hairlineSoft` (D10). El primitivo `DataTable` (CRUD: Servicios, Productos, Barberos) usa filas ~46–48px con separador `hairline` (más visible). | El admin corre también en **iPad** (D5): las tablas CRUD se tocan con el dedo, necesitan ≥44px de touch target (§5.4) y un separador claro que delimite dónde apretar. Las de reporte se consumen con mouse a distancia (escaneo de columna), donde la compactación pesa más. La regla D10 se matiza: report-style → `hairlineSoft`; CRUD táctil → `hairline`. |
| D10 | **Jerarquía visual de tablas densas del admin** — patrón "sin bandas grises, anclaje del total con acento indigo". Reglas: **(a)** headers de columna (`<thead>`): eyebrow mono uppercase **`inkSoft`** weight `heading`, sin bg, `border-bottom: 1px hairline`. **(b)** header del bloque (eyebrow del título de la card-tabla): mismo lenguaje que thead (inkSoft + heading, sin bg, border-bottom hairline). **(c)** filas de datos: `border-bottom: 1px hairlineSoft` entre filas, hover surfaceAlt scoped. **(d)** **fila TOTAL/subtotal**: `border-top: 2px solid theme.accent` (línea indigo gruesa que ancla la mirada), texto label en `ink` heading, monto en `accent` heading tabular-nums, **sin bg** (sin banda gris). | Aplicado uniformemente a Balances, Caja, Ventas, Gastos, Planillas. El gris claro (`surfaceAlt`) se usaba antes para 3 cosas distintas dentro de la tabla (header del bloque, thead, fila total) Y para el fondo del admin afuera. Resultado: las "bandas" se confundían con el fondo del admin y la jerarquía se perdía. La regla nueva delega la jerarquía a tipografía + peso de separadores. La **línea indigo de 2px en el total** es la pieza que ancla visualmente la mirada del cliente en el dato más importante de la tabla (lo que un dueño quiere ver al toque). Reemplaza la "banda gris" del subtotal/total que tenían todas las secciones. La excepción es el header clickable del bloque-día en Planillas (acordeón): se queda con `accentSoft` cuando es el día actual (jerarquía positiva), `surface` blanco el resto (el chevron + cursor da affordance de clickable). |
| D15 | **Sacar el `ScreenHeader` de `SeccionGestion`** (revierte D13 para esta sección). Gestión queda como 2ª sección sin header, tras Turnero (D11). Los Tabs underline pasan a ser el elemento superior. | Pedido explícito del usuario ("no lo quiero"). D13 ya anticipaba que el usuario evaluaba eliminar/repensar los headers; acá lo decidió para Gestión. **Pendiente abierto**: si se quiere coherencia total, sacar los headers del resto del admin es un **pase global** aparte (no decidido todavía). El sidebar ya indica en qué sección estás, así que el header aportaba poco. |
| D16 | **Layout de `TabNegocio` = un único bloque dividido**, no stack de cards scrolleable. Horario de atención como franja full-width arriba + Feriados \| Imágenes en dos columnas debajo, todo dentro de un panel `surface` con divisores `hairline`. Se **removió el formulario de nombre/URL** (gestión por DB). | El usuario no quería scrollear el contenido del tab; prefería ver todo dividido por sección en un bloque horizontal. Las 3 secciones del negocio son config relacionada → un solo panel "del negocio" se lee mejor que 3 widgets sueltos. La lista de feriados (largo variable) scrollea internamente para no romper el "sin scroll de página" (en pantallas de alto chico puede aparecer algo de scroll global igual — aceptado). |

---

## 1. Fases

### Fase 0 — Decisión arquitectónica ✅
- [x] Definir D2 (inline-styles) y D3 (coexistencia Tailwind).

### Fase 1 — Fundamentos ✅
- [x] Copiar `theme/tokens.js` desde `frontend-turnero` a `frontend/src/theme/tokens.js` (idéntico, con nota de que `maxWidth` no se usa en el shell admin).
- [x] Crear `frontend/src/utils/formato.js` (singular) **consolidado** — ver §2.1.
- [x] Crear `frontend/src/utils/fecha.js` (singular) **consolidado** — ver §2.1.
- [x] Reemplazar `frontend/src/index.css` por la versión de turnero (reset + Geist + keyframes), **manteniendo** el `@import "tailwindcss"` arriba mientras dure la coexistencia. Se conserva también el keyframe `spin` legacy hasta Fase 3 (lo usa el spinner ad-hoc de `App.jsx`).
- [x] Borrar `frontend/src/App.css` y todo import a él (verificado con grep: nadie lo importaba).
- [x] `frontend/index.html`: `lang="es"` y reemplazar el `<link>` de Raleway por el de Geist + Geist Mono.
- [x] Instalar `lucide-react` en `frontend/package.json` (lo necesita Fase 2).
- [x] **No tocar** los archivos viejos `utils/formatos.js` y `utils/fechas.js` todavía — quedan vigentes hasta que la Fase 4 vaya migrando los imports archivo por archivo. Eliminación final anotada en Fase 6 y deuda §3-8.

### Fase 2 — Primitivos universales ✅
- [x] Copiar a `frontend/src/components/ui/` los 13 primitivos universales de turnero (lista en sistema de diseño §6.1) **idénticos**. Se sumó también `IconoAlerta` (helper SVG que turnero exporta y usamos para el glyph del `EmptyState` de error).
- [x] Crear `frontend/src/components/ui/index.js` (barrel) con los exports.
- [x] Verificar focus visible en todos. Resultado: todos los interactivos (Button, Card, Field, TopBar, ConfirmDialog) quedan cubiertos por el `*:focus-visible` global de `index.css` (outline indigo 2px). Field además tiene ring propio en el input. **Sin deuda nueva por focus.**
- [x] **Diferido**: los ajustes de densidad compacta NO se aplican upfront. Se hacen puntualmente durante Fase 4 cuando una pantalla real lo pida. Si el ajuste sirve a 2+ pantallas, se sube al primitivo (regla §7.4 del sistema de diseño).

**Pendiente para Fase 3** (anotado también en el header de `PageContainer.jsx`): el primitivo es mobile-first con `maxWidth: 480`. No sirve tal cual para el shell admin horizontal. Decidir en Fase 3 entre:
- (a) modificar el primitivo agregando props `maxWidth`/`fluid` para que sirva a los dos fronts, o
- (b) crear un `AdminPageContainer` paralelo en `frontend/src/components/ui/` que copie el patrón pero adaptado al desktop.

### Fase 3 — Shell del admin ✅
**Alcance**: solo `App.jsx` y `PanelAdmin.jsx`. `MainScreen.jsx` queda **fuera** y se ataca en su propia fase con chat dedicado (ver Fase 5.5).

Decisiones tomadas:
- **PageContainer**: queda intacto (mobile-first). El admin tiene su propio wrapper raíz porque su layout es `flex row` (sidebar + contenido), no columna centrada con max-width. El primitivo lo usarán los logins en Fase 4.
- **Sidebar**: pasa a **claro** (coherente con el norte "Luz / Stripe/Clerk"). Reversible si en la práctica no convence.

Sub-pasos:
- [x] Refactor de `App.jsx`: `stylesEstado` eliminado. `PantallaCargando` ahora usa 3 `Skeleton` en columna centrada. `PantallaError` usa `EmptyState` + `IconoAlerta` + `Button`. Se sumó `nombreNegocio` al estado y se pasa como prop a `PanelAdmin` (resuelve deuda #10). El keyframe `spin` queda en `index.css` porque lo usan 4 secciones todavía — se borra al final de Fase 4.
- [x] Refactor de `PanelAdmin.jsx`: sidebar claro (`theme.surface` + border `theme.hairline`), tokens del theme, Geist, **iconos Lucide** (Home, DollarSign, ClipboardList, BarChart3, ShoppingBag, Receipt, Calendar, Settings, LogOut, ChevronLeft/Right, AlertTriangle, X), `onClick` en todos los botones, conservada funcionalidad de colapso (220 ↔ 64 px) y banner de aviso de pago (re-estilado con `theme.warning`/`theme.warningSoft`). Item activo: `bg theme.accentSoft` + `color theme.accent` + indicador lateral indigo de 3×18 px. Hover en navItems y botón cerrar sesión vía `useState` (8 items, costo trivial). Resuelve deuda #10 (sin fetch propio de getNegocio) y resuelve parcialmente deuda #11 (PanelAdmin); MainScreen pendiente en Fase 5.5.

### Fase 4 — Migración por sección
Orden acordado. **Los 3 flujos de `MainScreen` (Corte/Venta/Gasto) van al final.**

- [x] `PantallaLoginAdmin.jsx` y `PantallaLoginOperativo.jsx`. Cambios principales:
  - Logo en ambas pantallas viene de `getImagenesNegocio()` (filtro `tipo === 'logo'`, ordenado por `orden`). El campo `tenant.logo` legacy ya no se lee desde el front; queda anotado para limpieza al mergear feature/turnero a main (ver §3-12).
  - Login admin: `PageContainer` + `TopBar` con "Cancelar"/"Volver" + logo o `Lock` (Lucide) como fallback + título centrado + indicadores 4 puntos + numpad táctil 3×4 (`TeclaNum` local) + teclado físico. Pantalla bloqueada usa `EmptyState` + `IconoAlerta` + `Card` de contacto. `onPointerDown` → `onClick`. Animación shake migrada de inline `<style>` al keyframe `om-shake` en `index.css`.
  - Login operativo: `PageContainer` + logo o `Lock` fallback + `Field` (usuario, contraseña) + `Button`. Error inline en el subtitle con `theme.danger`.
  - Extensión del primitivo `Field`: nuevos props `disabled`, `autoComplete`, `autoCapitalize`, `autoCorrect`, `spellCheck`. Visual de disabled: fondo `surfaceAlt`, texto `muted`, opacity 0.7, cursor `not-allowed`. **Pendiente backport a turnero** (deuda §3-13).
  - Sumado `getImagenesNegocio()` a `services/api.js` (copiado del turnero). `App.jsx` ahora hace `Promise.all([getNegocio, getImagenesNegocio])` para cargar tenant + logo en paralelo.
- [x] `SeccionInicio.jsx` (validación del sistema con una sección chica). Cambios principales:
  - Layout: 3 cards apiladas verticalmente. `CardDia` + `CardMes` con altura igualada via grid `gridAutoRows: 1fr`; `CardStock` full-width abajo. Contenido centrado vertical y horizontalmente dentro de cada card y de cada `Metrica`.
  - Migración a primitivos: `ScreenHeader`, `Card`, `EmptyState`, `Button`, `IconoAlerta`. Sumado **`LoadingState`** nuevo (ver D6) — usado para el estado de carga; el error usa `EmptyState` + `IconoAlerta` + botón "Reintentar" (trigger por estado `intento` que re-ejecuta el efecto con guard de cancelación).
  - Tipografía Geist + tokens en todo. Eliminados colores hardcodeados (`#1a7a4a`, `#c0392b`, `#888`, `#eee`, etc.), fuente `DM Sans` y emojis (👤👥💵✓). Iconografía Lucide (`Users`, `DollarSign`, `Package`, `CheckCircle2`, `TrendingUp/Down`, `RefreshCw`).
  - `formatMonto` local eliminado — la sección ahora usa `fmtPesos` de `utils/formato.js`.
  - `BadgeVariacion` rediseñado (pill `successSoft`/`dangerSoft` + `TrendingUp`/`Down` + `%`). Estado "sin datos" pasa de pill gris a texto muted plano. **Queda local** a `SeccionInicio` — promoción a primitivo cuando aparezca el segundo uso (probable: Balances/Ventas).
  - Tabla de stock ad-hoc con grid 3 columnas y `tabular-nums`; el caso "stock en orden" usa `EmptyState` con `CheckCircle2`. `DataTable` se posterga a Fase 5 (regla §7.1: esperar al segundo uso real con requisitos de filtros/sort/paginación).
  - `PanelAdmin.jsx`: fondo del `<main>` pasa de `theme.bg` a `theme.surfaceAlt` (decisión D7). Las secciones no override.
  - Resuelve parcialmente la deuda #15 ("excepción spinner") al formalizar D6.
- [x] `SeccionCaja.jsx`. Cambios principales:
  - Nuevo primitivo **`Tabs`** (variante underline, Stripe/Clerk look — formaliza D8). Navegación por teclado WAI-ARIA (←/→, Home/End), soporte de ícono opcional por tab. Primer uso aquí, próximo esperado en `SeccionGestion`.
  - `ConfirmDialog` extendido con prop `children` opcional para renderizar detalle estructurado debajo del `message` (resumen del recurso a eliminar). Reemplaza el modal ad-hoc original. La divergencia con turnero no se considera deuda per D9.
  - Sub-componentes `BadgeFormaPago`, `TogglePill`, `BotonExportarExcel` rediseñados in-place con tokens + Geist + Lucide + `onClick`. **Sin** promoverlos a `/components/ui/` todavía (eso es Fase 5). `BadgeFormaPago` además resuelve un bug menor: forma desconocida ya no cae silenciosamente a "Mercado Pago", muestra el valor crudo con estilos neutros.
  - 6 callsites de `BotonExportarExcel` migrados de `onPointerDown` → `onClick` en Caja, Balances ×2, Planillas, Ventas, Gastos. `TogglePill` ya usaba `onToggle` (sin migración).
  - Imports migrados a `utils/formato.js` (singular) y `utils/fecha.js` (singular). En SeccionCaja se usa `fmtPesos` (consistente con SeccionInicio) en lugar de `formatARS`.
  - Layout: `ScreenHeader` ("Caja" / "Movimientos diarios y cierres") + `Tabs` + tab activo. Tab Movimientos: 2 cards de totales (efectivo via Lucide `Banknote`, MP via `<img>` real con fallback a `Banknote`), fila de acciones (toggle | SelectorDia | exportar), tabla densa o `EmptyState`. Tabs Cierre/Historial: `EmptyState` con glyph `Construction` y copy "Próximamente".
  - Tabla densa ad-hoc (sin `DataTable` aún, postergado a Fase 5 — ver deuda #20). Sin zebra, sin fila rosada de gasto — jerarquía por pill `TipoMovimientoPill` (local) + monto en `theme.danger` para gastos. Hover de fila vía `:hover` scoped con `<style>` inline (excepción consciente, ver deuda #21). Botón eliminar por fila: Lucide `Trash2` 16, hover a `danger` + `dangerSoft`.
  - Loading via `LoadingState` (D6). Error via `EmptyState` + `IconoAlerta` + botón "Reintentar" con trigger por estado `intento` y guard de cancelación (mismo patrón que SeccionInicio).
  - Comentario inline explicando la semántica del filtro "Solo Barberos" (saca cortes con comisión 100% porque van directo al bolsillo del barbero, no a la caja del local) — para que el próximo lector entienda sin contexto.
  - Resuelve deuda #11 para SeccionCaja (eliminado todo `onPointerDown`).
- [x] `SeccionVentas.jsx`. Cambios principales:
  - `ScreenHeader` ("Ventas" / "Productos vendidos por mes"), `LoadingState` (D6), error con `EmptyState` + `IconoAlerta` + botón "Reintentar" (trigger por `intento` y guard de cancelación, mismo patrón que Caja/Inicio), empty con `EmptyState` + Lucide `Package`.
  - Tabla densa ad-hoc (sin zebra, `:hover` scoped via `<style>` inline — excepción #21). `DataTable` postergado: la tabla no tiene sort/filtros/paginación, sigue siendo la misma complejidad que Caja, esperar a Balances (deuda #20).
  - Footer "Total del mes" en `theme.accent` (decisión nueva del chat: jerarquía del total via indigo + bold + tabular-nums, no via color verde). Mismo tratamiento en `TablaResumen` ("Total general").
  - Acciones por fila: nuevo sub-componente local `BotonIconoFila` (28×28, border `hairline`, hover muta border/bg/color según prop `tono` — `accent` para editar, `danger` para eliminar). Lucide `Pencil` + `Trash2`. Reemplaza los emojis ✎ ✕ y los `onPointerDown` del original.
  - `ModalConfirmarEliminar` ad-hoc → `ConfirmDialog` con `children` (`DetalleVentaConfirm` con Fecha/Producto/Cantidad/Total).
  - `ModalEditarVenta` re-armado como modal ad-hoc reutilizando el **shell visual** de `ConfirmDialog` (overlay `rgba(9,9,11,0.5)`, card `surface`, animaciones `om-overlay-in`/`om-dialog-in`, ESC cierra). Inputs con primitivo `Field` para Cantidad. Sub-componentes locales nuevos: `SelectFormaPago` (select nativo estilado con tokens, focus ring indigo) y `CampoFijo` (input no editable estético). Sin promover a `Modal` primitivo: esperar segundo form modal (deuda #23).
  - Helper interno `recalcularTotales(lista)` para evitar la duplicación que tenía el original entre `confirmarEditar` y `confirmarEliminar`.
  - Imports migrados: `utils/fecha.js` (singular, `mesALabel`, `getMesActual`) y `utils/formato.js` (singular, `fmtPesos` reemplaza `formatARS`).
  - Resuelve deuda #11 en SeccionVentas (eliminado todo `onPointerDown`). Eliminados todos los colores hardcoded (`#1a7a4a`, `#c0392b`, `#888`, `#eee`, etc.) y el spinner ad-hoc.
- [x] `SeccionGastos.jsx`. Cambios principales (cerrados en el mismo chat que Ventas para extraer primitivos comunes):
  - **4 primitivos nuevos** en `components/ui/` (cierre anticipado de la mayor parte de la deuda #23):
    - `Modal` — shell genérico de modal (overlay + card + animaciones `om-overlay-in`/`om-dialog-in` + ESC + click fuera + loading-guard). Lo usan los modales de edición de Ventas y Gastos. `ConfirmDialog` todavía no lo usa internamente — anotado como deuda #24.
    - `Select` — select nativo estilado con tokens (focus ring indigo, eyebrow uppercase). Pareja visual de `Field`. Usos: Ventas (forma_pago), Gastos (categoría dinámica + forma_pago).
    - `BotonIconoFila` — icon-button compacto (28×28) con caja para acciones de fila. Variantes por `tono` (`'accent'|'danger'`). Migrado a su vez en `SeccionCaja` (botón de eliminar fila) — **cierra deuda #22**.
    - `DetalleRecurso` — lista vertical de pares label/valor para insertar como `children` del `ConfirmDialog`. Migrado a su vez en `SeccionCaja` (reemplaza `DetalleMovimientoConfirm` local — ahora se arma vía helper `filasDetalleMovimiento(m)`).
  - `SeccionGastos`: mismo patrón estructural que Ventas — `ScreenHeader` + `SelectorMes`/Exportar + `LoadingState`/`EmptyState`(con `Receipt`)/error con `IconoAlerta` y `Reintentar` + `TablaGastos` ad-hoc densa con `:hover` scoped + `TablaResumen` + `ConfirmDialog` con `DetalleRecurso` + `ModalEditarGasto` (`Modal` + 2 `Field` + 2 `Select`).
  - Decisiones visuales nuevas (siguiendo recomendaciones del chat):
    - Monto por fila en `theme.danger` (semántica de egreso, consistente con la fila de gasto en Caja).
    - Total del mes y total general en `theme.accent` (regla "indigo para destacar totales" — heredada de Ventas).
    - Sub-componente local `BadgeCategoria` (pill neutro con tokens: `surfaceAlt`/`inkSoft`, sin border).
  - Sub-componentes locales eliminados de Ventas al promover primitivos: `SelectFormaPago` (→ `Select`), `BotonIconoFila` local (→ primitivo), `DetalleVentaConfirm` (→ filas inline + `DetalleRecurso`), shell ad-hoc del `ModalEditarVenta` (→ `Modal`). Queda local solo `CampoFijo` (único en Ventas hoy, deuda #23 reducida).
  - Imports migrados: `utils/fecha.js` y `utils/formato.js` (singulares). Eliminados `onPointerDown`, emojis, DM Sans, todos los colores hardcoded (`#1a7a4a`, `#c0392b`, `#888`, `#eee`...) y el spinner ad-hoc.
  - Resuelve deuda #11 en SeccionGastos (sin `onPointerDown`).
- [x] `SeccionPlanillas.jsx`. Cambios principales:
  - Estructura: `ScreenHeader` ("Planillas" / "Cortes y comisiones por barbero, semana a semana") + primitivo `Tabs` underline (Detalle/Resumen) + fila de controles (`TogglePill` Con/Sin comisiones | `SelectorSemana` | `BotonExportarExcel`). Mismo patrón estructural que Caja/Ventas/Gastos.
  - Tab Detalle: barra de chips de barberos (filtro, no navegación — ver decisión nueva más abajo) + lista de bloques colapsables por día. Cada `BloqueDia` es una card con header clickable (fecha + pill "HOY" indigo + cantidad de cortes + chevron Lucide `ChevronRight`/`ChevronDown`), tabla expandible y `tfoot` con subtotal siempre visible. Por defecto solo hoy arranca expandido (al cargar la semana o al cambiar de barbero).
  - Tab Resumen: sub-componente `TablaResumen` densa con una fila por barbero + fila TOTAL. Comisión por fila en `theme.accent` con eyebrow mono mostrando el tipo (`XX%` o `$N/c`).
  - **Sub-componente local nuevo: `ChipBarbero`** — chip de filtro (radius pill, padding 6/12). Inactivo: transparente con border `hairline` + texto `inkSoft` (hover bg `surfaceAlt`). Activo: bg `accentSoft` + border `accent` + texto `accent` weight heading. `aria-pressed` para accesibilidad. Hover via `useState` local (costo trivial dado N de barberos ≤ ~10). Primer caso del patrón "chip de filtro" en el admin — **promover a primitivo cuando aparezca un segundo uso** (§7.1, anotado como deuda #26).
  - Decisión nueva: **barberos como chips de filtro, no como segunda barra de Tabs**. Probamos primero con dos barras de Tabs underline apiladas (consistente con D8) y el usuario eligió chips. Razón: conceptualmente los barberos son "filtro" del contenido que muestra el tab Detalle, no "navegación" entre vistas distintas. Dos barras de tabs apiladas se sentían pesadas. La consistencia visual del sistema se sostiene igual (radius pill, tokens accent/hairline, mismo lenguaje cromático).
  - Visual: bloques de día con border `hairline` + radius `radiusLg`. Header del bloque cuando esHoy: bg `accentSoft` + texto fecha en `accent` + pill "HOY" sólida indigo (eyebrow mono uppercase). Resto de días: bg `surfaceAlt`. Tabla densa (padding 8/14) con th eyebrow mono uppercase + td body + `:hover` scoped (`.om-planillas-fila`, excepción consciente §4.2, deuda #21). Subtotales y totales en `theme.accent` con tabular-nums (consistente Ventas/Gastos). El slot "Pago" del subtotal del día mantiene la semántica original (`comisión + propinas` con eyebrow del tipo de comisión al lado) por pedido explícito del usuario — ver deuda #27.
  - `BadgeFormaPago` importado en la tabla del día — **4to uso** del componente (Caja, Ventas, Gastos, Planillas). Sigue local en `components/`, no promovido a primitivo (sigue en Fase 5 explícita).
  - Loading via `LoadingState` (D6). Error con `EmptyState` + `IconoAlerta` + botón "Reintentar" (`intento` + guard `cancelado`, mismo patrón Caja/Ventas/Gastos). Empties con `EmptyState` + Lucide `Scissors`.
  - Imports migrados: `utils/fecha.js` y `utils/formato.js` (singulares). `formatARS` → `fmtPesos`. Eliminados todos los `onPointerDown` (3 callsites: 2 tabs viejos + toggle día), DM Sans, colores hardcoded (`#1a7a4a`, `#888`, `#999`, `#f0faf5`, etc.), spinner ad-hoc y `<p>Cargando...</p>` / `<p>Error</p>`.
  - Sub-componentes locales del archivo: `BloqueDia`, `TablaResumen`, `ChipBarbero`, `ThCelda`, `TdCelda` (los dos últimos son cell helpers densos reusables dentro de la sección).
  - Resuelve deuda #11 en SeccionPlanillas (sin `onPointerDown`).
- [x] `SeccionBalances.jsx`. Cambios principales:
  - Estructura: `ScreenHeader` ("Balances" / "Visualización mensual de la economía del negocio") + primitivo `Tabs` underline (Balance mensual / Histórico anual) + fila de controles por tab. Migra de tabs segmented pill ad-hoc al patrón D8.
  - **Primitivo nuevo: `BadgeVariacion`** promovido desde `SeccionInicio` a `components/ui/BadgeVariacion.jsx` (segundo uso real: columna "vs anterior" del histórico). Versión idéntica a la local — pill `successSoft`/`dangerSoft` + Lucide `TrendingUp`/`Down` + `%`, fallback texto muted con `sinDatosLabel`. `SeccionInicio` migrada al primitivo (eliminada la función local). **Cierra deuda #16.**
  - Tab Mensual: 3 KPI cards (Ingresos / Egresos / Balance neto) re-armadas con tokens, sobre `theme.surface` con border `hairline`. Cada KPI lleva eyebrow mono uppercase + ícono Lucide (`Banknote`/`Receipt`/`Wallet`) en caja `surfaceAlt` 28×28 + valor 26px tabular-nums + subtítulo `muted`. **Decisión de color del KPI Balance neto**: positivo en `theme.accent`, negativo en `theme.danger` (opción b del chat — conserva la señal del signo dentro del lenguaje del sistema, sin verde). Ingresos siempre `accent`, Egresos siempre `danger`.
  - Tab Mensual sigue con 2 bloques: "Ingresos por barbero" + "Egresos por categoría" (condicional). Ambos en `Card` con header eyebrow mono uppercase + tabla densa ad-hoc. Fila "Productos vendidos" sin bg propio (se distingue por la primera columna en `muted`). Fila TOTAL con bg `surfaceAlt` + border-top `hairline`. Por fila: ingreso/neto en `theme.accent`, comisión/egreso en `theme.danger`, todo con `tabular-nums`. Sub-componente `BloqueTabla` local con `header`/`footer` opcionales — el footer aloja la nota de propinas.
  - Nota de propinas: Lucide `Info` 14 + texto en `muted` con monto en `ink` bold tabular-nums. Reemplaza el emoji 💡 (§3.7).
  - Tab Histórico: bloque + tabla densa de 12 filas (sub-componente `TablaHistorico`). Fila del mes actual: bg `accentSoft` + pill **"ACTUAL"** sólida (`accent` bg + `accentInk` color + eyebrow mono uppercase) — mismo patrón visual que la pill "HOY" de Planillas. La columna "vs anterior" usa el primitivo `BadgeVariacion` con `sinDatosLabel="—"`. Balance por fila: positivo en `accent`, negativo en `danger` (consistente con KPI). Empty (`datosHistorico.length === 0`) con `EmptyState` + Lucide `BarChart3`.
  - Helper local `balanceSegunModo(fila, mostrarComisiones, usarBalancePrecalculado?)` para encapsular la fórmula del balance según el toggle (estaba duplicada en 3 lugares en el original). Acepta el switch de "usar `balance_neto` ya calculado" para el resumen mensual.
  - Loading via `LoadingState` (D6). Error con `EmptyState` + `IconoAlerta` + botón "Reintentar" (`intento` + guard `cancelado`, mismo patrón Caja/Ventas/Gastos/Planillas) — separado entre `intentoMensual` y `intentoHistorico` para que cada tab maneje su propio reintento. Empty del tab mensual (sin movimientos) con `EmptyState` + Lucide `Wallet` (sustituye el `<p>Sin movimientos en ...</p>` original).
  - **Bug fixeado del original (línea 370)**: `<p>Sin movimientos en {labelMes(mesSeleccionado)}.</p>` llamaba a `labelMes`, función que **no existía** en el archivo (ni importada ni definida). Era un `ReferenceError` latente que se gatillaba en el caso vacío. Reemplazado por `mesALabel(mesSeleccionado)` (correcto) dentro del nuevo `EmptyState`.
  - Imports migrados: `utils/fecha.js` (singular, `mesALabel`/`getMesActual`) y `utils/formato.js` (singular, `fmtPesos` reemplaza `formatARS`). Eliminado el sub-componente local `KpiCard` que usaba `formatARS` directo — el nuevo usa `fmtPesos`.
  - Eliminados todos los `onPointerDown` (2 callsites de tabs viejos), DM Sans, colores hardcoded (`#1a7a4a`, `#c0392b`, `#888`, `#999`, `#ccc`, `#eee`, `#fafafa`, `#f0faf5`, `#f9f9f9`, `#f5f5f5`, etc.), badges con flechas Unicode (`▲`/`▼`) y los `<p>Cargando...</p>` / `<p>{error}</p>` planos.
  - Tabla densa con `:hover` scoped via `<style>` inline (`.om-balances-fila`) — excepción consciente §4.2, mismo patrón Caja/Ventas/Gastos/Planillas (deuda #21).
  - Evaluación de disparadores:
    - **`DataTable` (deuda #20)**: las 3 tablas de Balances (por barbero, por categoría, histórico 12 meses) son densas pero **sin sort, sin filtros, sin paginación** — misma complejidad que Caja/Ventas/Gastos/Planillas. **Postergado de nuevo**, el caso real va a ser `SeccionGestion` (CRUD con filtros y N filas) o `SeccionTurnero`.
    - **`ChipBarbero` → `ChipFiltro` (deuda #26)**: Balances no tiene filtros multi-opción (solo toggle binario Con/Sin comisiones + selector de mes). **Postergado**, esperando segundo uso real.
    - **Charts**: el original no los tiene; no se agregaron — el rediseño visual es 1:1. El tab Histórico queda como candidato natural a un line chart cuando se decida (no hay decisión vigente en el sistema sobre librería).
  - Resuelve deuda #11 en SeccionBalances (sin `onPointerDown`).
- [x] `SeccionTurnero.jsx`. Cambios principales:
  - **No es un rediseño 1:1**: se replanteó la sección a fondo. Pasa de "tabla agrupada por barbero" a **doble vista (Agenda / Lista)** con toggle, agenda como default. Agenda = grilla CSS hecha a mano (D12) — eje vertical horas, columnas barberos, bloques dimensionados por duración real, línea "ahora" indigo (60s refresh) cuando la fecha es hoy. Lista = tabla densa D10 con click en fila para abrir el mismo modal de detalle.
  - **Sin `ScreenHeader`** (D11) — primera y única sección del admin sin header, para maximizar verticalidad de la agenda.
  - **Primitivo nuevo**: `ChipFiltro` promovido desde `ChipBarbero` local de `SeccionPlanillas` (segundo uso real: filtro de barberos en Turnero). API extendida con prop `size: 'md' | 'sm'` — `sm` se usa para filtros secundarios (estado), con padding más ajustado y weight fijo en `medium` (no sube a `heading` cuando activo) para señalar jerarquía menor sin ocultar el filtro. `SeccionPlanillas` migrada al primitivo (definición local eliminada). **Cierra deuda #26.**
  - **KPIs del día**: 4 mini-cards arriba (Total / Reservados / Completados / Sin éxito = no_asistio + cancelado). Calculados sobre el dataset completo del día (NO respetan los filtros) — la idea es que sean siempre representativos de "lo que hay en el día". Colores: total `ink`, reservados `accent`, completados `success`, sin éxito `muted`.
  - **Filtros con dos niveles de jerarquía**: chips de barbero (primarios, `ChipFiltro size="md"`) y chips de estado (secundarios, `ChipFiltro size="sm"` con eyebrow mono "ESTADO" delante). El filtro por barbero se aplica server-side (parámetro del fetch); el filtro por estado se aplica client-side para evitar refetch en cada toggle.
  - **Mapeo de estado del dominio admin** (sub-componente local `EstadoTurnoPill`): `reservado` → `accent` (indigo, "abierto"), `completado` → `success` (verde), `no_asistio` → `warning` (amarillo), `cancelado` → `muted` (gris). Diferente del primitivo `StatusPill` que mapea para el flujo cliente del turnero (allí `reservado` = success). Per D9, está OK divergir — el primitivo se reserva para el otro contexto.
  - **Modal de detalle único**: reemplaza al `ModalCancelarTurno` ad-hoc. Usa primitivo `Modal` + `DetalleRecurso`. Adentro tiene dos "modos": `detalle` (lista de campos + acciones) y `confirmando` (paso de confirmación de cancelación con `Button` variant=`danger`). El switch entre modos vive dentro del propio `Modal` (sin anidar modales). El bloque de contacto del cliente (teléfono + email) usa un sub-componente local `FilaDetalleConCopia` con botón **Copiar al portapapeles** (navigator.clipboard) y feedback "Copiado" 1.5s — info subutilizada del endpoint que ahora queda accionable en un click.
  - **Acciones por turno** (solo si está `reservado`): Marcar completado (primary indigo), No asistió (secondary), Cancelar turno (ghost con color `danger`, alineado a la derecha con `marginLeft: auto` para separarlo visualmente como destructiva). Reemplazan los emojis ✓ ✗ 🗑 del original.
  - **Sub-componente local `SegmentedVista`**: segmented pill 2-opciones (Agenda / Lista) con tokens del sistema. Lucide `CalendarDays` / `Hourglass`. Local — segundo uso de "segmented binario" en el admin (el primero es `TogglePill` de Caja, distinto patrón); promover si aparece tercer caso.
  - **Banner inline de errores de acción** en lugar de `alert()`. Sub-componente local `BannerError` con `dangerSoft` + `AlertTriangle` + botón X. Auto-dismiss en 6s vía `useRef(setTimeout)`. **Deuda nueva #30**: construir primitivo `Toast` cuando aparezca el segundo caso.
  - **Rango horario de la agenda derivado del dataset**: `min(inicio)` y `max(fin)` con padding 1h por extremo, clamp externo a 08:00–22:00 (la agenda se expande pero nunca se contrae por debajo del default). Fallback puro 08:00–22:00 si no hay turnos. **Deuda nueva #31** (backend): exponer `horario_atencion` global del tenant en `getNegocio()` para que la agenda muestre la jornada real aun sin turnos cargados.
  - **`AvatarIniciales`** usado por primera vez en el admin: en el header de cada columna de la agenda y junto al barbero en la fila de la lista. Da escaneo visual rápido en agendas con varios barberos.
  - **`BadgeFormaPago` no se usa**: el endpoint `/admin/turnos` actualmente NO devuelve `forma_pago` por turno. **Deuda nueva #32** (backend): sumar `forma_pago` al endpoint admin para turnos `completado` y mostrarla en el modal de detalle. Mientras tanto, el modal omite ese campo.
  - **`DataTable` postergado nuevamente**: la lista de Turnero es densa pero sin sort/filtros/paginación complejos (los filtros viven afuera como chips). Mismo caso que las anteriores. La espera real sigue siendo `SeccionGestion`.
  - **Helpers de tiempo locales**: `minutosDelDia(iso)`, `minutosAhoraEnTZ()`, `calcularRangoAgenda(turnos)`, `duracionMinutos(turno)`, `formatDuracion(mins)`, `esHoy(fechaStr)`. Reutilizables — si Gestión (calendario por barbero) los necesita, candidatos a `utils/agenda.js`.
  - **Imports migrados** a `utils/fecha.js` (singular). Eliminados todos los `onPointerDown` (4 callsites: modal + 3 botones por fila), DM Sans, colores hardcoded (`#1a7a4a`, `#c0392b`, `#1565c0`, `#e3f2fd`, `#fafafa`, etc.), emojis (✓ ✗ 🗑 en acciones), `alert()` y los `<p>Cargando…</p>` / `<p>{error}</p>` planos.
  - **Resuelve deuda #11** en SeccionTurnero (sin `onPointerDown`). **Cleanup de paso**: se sacó un `fecha` desestructurado nunca usado en `SeccionPlanillas.jsx:150` (detectado por ESLint durante el chat).
- [x] `SeccionGestion.jsx` + tabs. **Partido en dos chats** por superficie. **✅ COMPLETA** (las 6 tabs en el sistema de diseño).
  - [x] **Chat A** — shell de `SeccionGestion` + `TabServicios` + `TabProductos` + `TabTurnero` + `TabSeguridad`. Cambios principales:
    - **Shell**: migrado de tabs segmented-pill ad-hoc (con emojis) a `ScreenHeader` ("Gestión") + primitivo `Tabs` underline (D8) en fila propia. 6 tabs con íconos Lucide (`Scissors`, `ClipboardList`, `Package`, `Calendar`, `Building2`, `Shield`). Primer caso del primitivo `Tabs` con 6 tabs — escala bien. Padding del wrapper `24px 32px` (mismo patrón que el resto del admin; el `<main>` del PanelAdmin **no** aplica padding, solo fondo). `onPointerDown` → el primitivo usa `onClick`.
    - **Primitivo nuevo `DataTable`** (cierra deuda #20): sort por columna (toggle binario asc/desc, flecha visible solo en columna activa + hint a 0.4 opacidad al hover), paginación (`pageSize`, footer solo si `rows.length > pageSize`, reset a pág. 1 al cambiar sort), `onRowClick` (fila como `role=button` + Enter/Space + hover `surfaceAlt` scoped por `useId`), prop `col.grow` (columna que absorbe el sobrante; las demás llevan `whiteSpace: nowrap` → ancho estable entre sorts/páginas — **fix del salto horizontal de columnas** que reportó el usuario), `tabular-nums` global. Filas ~46–48px (§5.4 touch iPad) con separador `hairline` (no `hairlineSoft`) — **divergencia consciente de D10 para tablas CRUD táctiles** (ver D14).
    - **Primitivos promovidos**: `BadgeEstado` (pill Activo/Inactivo) y `ToggleEstado` (par de chips Activo/Inactivo para form modal) desde local de `TabServicios` al aparecer 2do uso en `TabProductos` (§7.1). `Toast` (feedback efímero con auto-dismiss + variantes success/danger/warning/info + `dismissible`) construido al aparecer el 3er caso real — **cierra deuda #30 (parte auto-dismiss)**. `SeccionTurnero` migrada al `Toast` (eliminado `BannerError` local + su `useRef`/`setTimeout`).
    - **Primitivo extendido**: `Field` suma prop `inputMode` (passthrough). Todos los inputs numéricos (Precio, Stock) migrados de `type="number"` → `type="text" inputMode="numeric"` + filtro `\D` en onChange. Resuelve: (a) numpad nativo en iPad, (b) ya no acepta "e"/"+"/"-", (c) placeholder renderiza limpio.
    - **`TabServicios`**: `DataTable` (Nombre `grow` / Precio `fmtPesos` right / Estado right con `BadgeEstado`), click en fila → modal de edición (`Modal` + `Field` + `ToggleEstado`), empty `ClipboardList`, sin columna de acciones. Sin eliminar físico.
    - **`TabProductos`**: `DataTable` 5 columnas. Alerta de stock bajo en la celda "Stock actual" (`danger` + `AlertTriangle`, helper `tieneStockBajo`) — fila **no** se tinta. Modal con bloque de stock en mini-card neutra. **Cambio UX**: en creación pide "Stock inicial" (no "Stock mínimo"); el mínimo se ajusta recién en edición. Stock inicial / unidades a agregar resuelven con el mismo endpoint `/agregar-stock`.
    - **`TabTurnero`** (config booking online, NO la sección Turnero del admin): card de config con `Field` numérico (slot 1–240) + `MensajeFeedback` local (feedback inline persistente, NO Toast — patrón distinto) + `Button`. Wrapper scalable para futuras cards de config.
    - **`TabSeguridad`**: 2 cards (`CardConfig` local) PIN admin / Modo operativo, filas `FilaCredencial` local con `BotonIconoFila` (Pencil accent), `PillSinConfigurar` warning. 3 modales (PIN / usuario / contraseña) migrados a `Modal` + `Field` + `Button`. `Toast` success al guardar. Error de submit con `Toast` danger dismissible inline.
  - [x] **Chat B** — `TabBarberos` (✅) + `TabNegocio` (✅, + `BloqueHorarioAtencion`, `BloqueFeriados`, `BloqueImagenes`).
    - [x] **`TabBarberos`** rediseñado. Arquitectura **Opción C**: lista en `DataTable` + click en fila → modal chico de datos básicos (`ModalDatosBarbero`: nombre, PIN, comisión, estado) + acción de fila (`BotonIconoFila` `CalendarClock`) que abre un **modal ancho "Horario y ausencias"** (`ModalAgenda`) con `Tabs` internos (Horario / Ausencias), montados a la vez vía toggle de `display` para no refetchear al cambiar de tab.
      - **Columnas**: Nombre (`grow`, `AvatarIniciales` + nombre, left) / Comisión (`${entero}%`, right) / **Horarios** (nueva — check `Check` verde si tiene bloques cargados, centrado vía flex porque un svg no centra con `text-align`; sortable) / Estado (`BadgeEstado`, right) / acción (right, 56px). Comisión **siempre entera** (filtro `\D`, 0–100).
      - **Precarga de horarios** de todos los barberos en el mount con `Promise.allSettled` (N chico): alimenta el check de la columna **y** sirve de cache para abrir el modal de Horario sin refetch (un fallo puntual deja ese barbero con horarios "desconocidos" → "—", no rompe la tabla). Cierra el flag chat-local "fetch por-barbero sin cache".
      - **Sub-panel Horario** (PUT reemplaza el horario completo): días en **grilla horizontal** (`repeat(auto-fill, minmax(260px,1fr))`, `alignItems:start`) dentro de un `Modal` `maxWidth={900}` — antes era un stack vertical de 7 días que desbordaba la pantalla (disparó la deuda #38). "Agregar bloque" arranca con el **horario del local** de ese día (de `GET /admin/horario-atencion`); si el local cierra ese día o falla la lectura, cae a 09:00–18:00. **Días cerrados del local quedan bloqueados** (card atenuada `surfaceAlt`, label "Local cerrado", sin botón de agregar) — `horarioLocal` arranca en `null` = "desconocido" (no bloquea nada) y solo bloquea cuando se leyó la atención (degradación segura ante fallo de red). Mensaje de solapamiento del backend **humanizado** (`"día 5"` → `"Viernes"`, helper `humanizarDiasEnMensaje` — deuda #39). Botón Guardar alineado a la derecha; "Cerrar" del footer también.
      - **Sub-panel Ausencias** (rename de "Suspensiones" por pedido): lista + alta (maneja el conflicto 409 de turnos a cancelar con confirmación inline) + baja con confirmación **inline de 2 pasos** (no `ConfirmDialog` anidado dentro del `Modal`). Valida rango `desde < hasta` antes de habilitar el alta (cierra flag chat-local). Guards de cancelación con `vivoRef` (seteado a `true` al montar para robustez ante StrictMode — bug detectado y corregido en autorrevisión).
      - `InputTiempo` (input nativo `time`/`datetime-local` estilado como `Field`, focus ring indigo) vive **local** en el archivo — candidato a primitivo (deuda #37; `BloqueHorarioAtencion` también lo necesita).
    - [x] **`TabNegocio`** rediseñado (cierra Chat B y `SeccionGestion`). **No es 1:1** — se replanteó UX/layout a fondo. Cambios principales:
      - **Rename del tab**: "Datos del negocio" → **"Negocio"** (era la única tab multi-palabra; ahora todas son de una palabra y respetan el ritmo del tablist). Ícono `Building2` sin cambios.
      - **Se quitó el formulario de nombre del negocio + URL de reservas**: esos datos se administran **directo en la DB** (cambian muy poco; decisión del usuario). El endpoint `GET/PUT /admin/negocio` **queda** (lo usan `App.jsx` para título/logo y `PanelAdmin` para el nombre del sidebar) — solo desapareció el formulario. `TabNegocio` pasó a ser un **contenedor de layout puro sin fetch propio**. Trade-off: sin self-service para renombrar el negocio / cambiar la URL (deuda/nota #43).
      - **Header de `SeccionGestion` eliminado** por pedido del usuario (D15, revierte D13). Gestión queda como 2ª sección sin `ScreenHeader` (tras Turnero/D11).
      - **Layout (D16)**: un **único bloque** `surface`+`radiusLg`+`overflow:hidden` dividido con `hairline` — Horario de atención como **franja full-width** arriba (borde inferior) + debajo dos columnas **Feriados | Imágenes** con divisor vertical (wrap a 1 col en pantallas angostas). Reemplaza el stack vertical de 3 cards scrolleables que el usuario no quería.
      - **`BloqueHorarioAtencion`**: card-less + **grilla horizontal** de mini-cards por día (`auto-fill`, minmax 150px): nombre + toggle local `ToggleAbierto` (Abierto=accent / Cerrado=neutro — no se usó `ToggleEstado` por tener labels fijos) full-width + 2 `InputTiempo` apilados, validación de rango por card. Modal de cascada 409 ad-hoc → **`ConfirmDialog` + `DetalleRecurso`** (Turnos a cancelar en `danger`, Bloques afectados). **Lógica intacta**: `claveHorario` (detección de cambios sin contar horas fantasma de días cerrados), `normalizarSemana` (recorta segundos), validación `fin <= inicio`, flujo `ejecutarGuardado(confirmarCascada)` con rama 409. **No se tocó el shape de la respuesta** del endpoint (lo consume TabBarberos).
      - **`BloqueFeriados`**: card-less. Alta (`ModalAgregarFeriado` ad-hoc) → primitivo **`Modal`** + `InputTiempo type="date"` (`min={hoy}`, label inline) + `Field`; cascada 409 (`requiere_confirmacion`) y borrado → **`ConfirmDialog`** (+`DetalleRecurso` en la cascada); error de duplicado/validación como `Toast` dentro del modal. Lista con `BotonIconoFila` danger (`Trash2`) y **scroll interno** (`max-height:260` + `overflowY:auto`) — divergencia consciente de "las secciones no tienen overflow propio" (convenciones §Scroll global), justificada por ser lista de largo variable dentro de un bloque sin scroll. Helpers conservados (`hoyISO`, `formatFecha`, `insertarOrdenado`) y las dos ramas de 409.
      - **`BloqueImagenes`**: card-less + **miniaturas (76px) + un único tile "+"** por grupo (en vez de los 8 cuadrados fijos vacíos), que aparece solo si el grupo no llegó a su tope (helpers `imagenesDe`/`ordenLibre`). Click en miniatura = reemplazar esa posición; "+" = subir a la 1ª libre; tile "Subiendo…" para la nueva. Borrado (`ModalConfirmarEliminar` ad-hoc) → **`ConfirmDialog`**; overlay de borrado `×` ahora `<button>` con `X` Lucide sobre `rgba(9,9,11,.55)`; slots vacíos = `ImagePlus`. **Compresión WebP intacta** (`OPCIONES_COMPRESION`, input file oculto compartido, `handleArchivo`). Accesibilidad: miniatura `role=button`+teclado, tile "+" es `<button>`.
      - **Feedback** unificado con `Toast` (success auto-dismiss / danger dismissible) en los 3 bloques; carga con `LoadingState` (D6) + Reintentar (`intento`). Eliminado de los 4 archivos: `onPointerDown`, DM Sans, colores hardcoded (`#1a7a4a`, `#c0392b`, `#fafafa`, `#eeeeee`, etc.), `✓`/`×`/`+` textuales.
      - **Primitivo nuevo `InputTiempo`** promovido desde `TabBarberos` (ver Fase 5; cierra deuda #37) — genérico para `time`/`datetime-local`/`date` + props `min`/`max`/`step`/`disabled`. `TabBarberos` migrado (definición local eliminada).
- [x] `FlujoCorte.jsx`, `FlujoVenta.jsx`, `FlujoGasto.jsx` (último). **✅ — cierra Fase 4.** Cambios principales:
  - **Decisión de diseño (opción B)**: el contenido de cada paso vive en un **panel claro** (`surface`, `radiusLg`, casi full-screen — `calc(100vw/vh − 72px)`) centrado sobre el **fondo del local** (foto blur + velo), el mismo de MainScreen. Se descartó la opción A (contenido flotando sobre el velo) por legibilidad de un wizard multi-campo y para reusar el sistema tal cual adentro del panel.
  - **Primitivo nuevo `FondoLocal`** (`components/ui/FondoLocal.jsx`): encapsula la capa de imagen (blur + scale 1.1) + velo `rgba(9,9,11,0.45)` + fallback `surfaceAlt` + constantes `VELO`/`BLUR_PX`. **`MainScreen` refactorizado** para usarlo (sin cambio visual). `App.jsx` pasa `imagenLocal` a los 3 flujos.
  - **Módulo compartido `flows/wizard.jsx`** (NO en `components/ui/` — superficie kiosko-wizard, mismo criterio que los wizard-only del turnero §6.2): `PanelWizard` (panel + barra de progreso indigo + header con título centrado vía grilla `1fr/auto/1fr` + separador indigo 4px), `BotonOpcion` (card táctil, activo `accentSoft`+`accent`, prop `dashed` para "Sin turno"), `CampoTexto` (textarea con lenguaje de `Field`), `CampoMonto` (input `text`+`inputMode=numeric`+filtro `\D`, numpad iPad), `FilaResumen`/`FilaTotal` (densidad kiosko, total en `accent` ingreso / `danger` egreso), `PantallaExito` (checkmark `om-pop` + monto), `wizardStyles`. Los 3 flujos quedaron a pura lógica + lo específico de cada uno.
  - **Transición entre pasos simplificada**: se sacó el `navigate` de 3 fases (slide + refs + timers); ahora es cambio de paso instantáneo + `om-fade` vía `key={paso}` (animación funcional del sistema).
  - **Migración de utils (desbloquea Fase 6 Etapa B)**: `FlujoCorte` pasó `getFechaHoy`/`formatHora` de `utils/fechas` (plural) → `utils/fecha` (singular). `formatHora` es idéntica (TZ fija canónica) → cero cambio de comportamiento. Era el **último importador** de `fechas.js`, ahora eliminado.
  - Eliminado de los 3 archivos: `DM Sans`, `clamp()`/`vw`/`vh`, verde `#1a7a4a` y grises hardcoded, emojis (💵✅❌📦), `onPointerDown` (→ press vía clase scoped `om-opcion`), `<input type=number>`, `<p>Cargando…</p>`/`<p>{error}</p>` planos. **Resuelve deuda #11 en los flujos.**
  - Forma de pago con Lucide `Banknote` + `<img>` real de Mercado Pago (fallback). Cleanup de paso: guard de cancelación (`cancelado`) en el `useEffect` de turnos de `FlujoCorte`; `respuesta` sin usar eliminado en `FlujoVenta`.

### Fase 5.5 — Rediseño de MainScreen (chat dedicado)
**Pantalla operativa del local (la "home" del iPad).** Decisión del usuario: se ataca en su propio chat porque además del rediseño visual se va a sumar la imagen del local + logo, y se quiere dedicar tiempo a que quede bien.

Pendientes a resolver en ese chat:
- Migración de los 3 botones gigantes (Corte / Venta / Gasto) al sistema (probablemente como cards de acción locales a MainScreen — regla §7.1).
- Decisión final sobre los colores actuales (verde/rojo) vs el "solo indigo" del sistema. Opciones planteadas en este chat: (a) todo indigo, (b) mantener verde/rojo como excepción consciente, (c) usar `theme.success` / `theme.danger` como acentos de acción.
- Sumar foto del local + logo al diseño.
- Eliminar `onPointerDown` (§4.3) y `clamp()`/vh-vw arbitrarios.

### Fase 5 — Primitivos específicos del front gestión
A construir cuando aparezcan durante Fase 4 (regla §7.5 del sistema de diseño: cuando el primer caso real lo necesite).

- [x] **`DataTable` denso** — construido en chat A de Gestión (filas ~46–48px touch, sort + paginación + `onRowClick` + `col.grow`). Cierra deuda #20. Usado en `TabServicios`, `TabProductos` y `TabBarberos`.
- [x] **`InputTiempo`** — promovido a `components/ui/InputTiempo.jsx` en el chat de `TabNegocio` (cierra deuda #37). Genérico para `time` / `datetime-local` / `date` + props `min`/`max`/`step`/`disabled`. Mismo look que `Field` (border hairline, focus ring indigo). Usos: `TabBarberos` (horario barbero `time` + ausencias `datetime-local`), `BloqueHorarioAtencion` (`time`), `BloqueFeriados` (`date`). `TabBarberos` migrado (definición local eliminada).
- [x] **Evaluada la consolidación** de `SelectorMes` / `SelectorSemana` / `SelectorDia` / `SelectorPeriodo` en un `PeriodSelector` único → **conclusión: NO se consolida**. Los 3 wrappers no comparten código entre sí (cada uno usa funciones de fecha distintas: `desplazarMes`/`desplazarDia`/`desplazarSemana`, labels propios) y el día diverge estructuralmente (compara `=== hoy` + `permitirFuturo` vs `>= actual` de mes/semana). Un componente único con `tipo=` sería un `switch` de ramas irregulares con props que solo aplican a algunos tipos — regresión, no mejora. La arquitectura **base visual (`SelectorPeriodo`) + wrappers de dominio finos** ya es el patrón correcto de composición. En cambio: **(a)** se **migró `SelectorPeriodo` a tokens** — estaba sin migrar (DM Sans, colores hardcoded `#1a7a4a`/`#e0e0e0`/etc., `onPointerDown`), o sea renderizaba con el visual viejo en las 6 secciones que lo usan; ahora Geist + tokens + chevrons Lucide (`ChevronLeft`/`Right`) + `onClick` + sub-componente `BotonNav` con hover propio + badge "destacado" en indigo sólido (mismo lenguaje que pills "HOY"/"ACTUAL"). **(b)** Se **promovió la familia a `components/ui/`** (los 4 archivos juntos; barrel exporta los 3 wrappers, `SelectorPeriodo` queda como base interna).
- [x] **Migrar a primitivos**: `TogglePill`, `BadgeFormaPago`, `BotonExportarExcel` → **movidos a `components/ui/`** + barrel + 5 callsites actualizados. Ya estaban migrados a tokens (sin cambio visual). Comentario de cabecera del barrel actualizado: dejó de ser "solo los 13 primitivos universales", hoy es la superficie de import de todos los componentes compartidos del admin (universales + admin-domain).
- [x] **Adelantado en chat de Gastos**: `Modal`, `Select`, `BotonIconoFila`, `DetalleRecurso` ya viven en `components/ui/` (4 primitivos nuevos).
- [x] **Adelantado en chat A de Gestión**: `DataTable`, `BadgeEstado`, `ToggleEstado`, `Toast` ya viven en `components/ui/` (4 primitivos nuevos). `Field` extendido con `inputMode`.

### Fase 6 — Cleanup final
**Partida en dos etapas** porque Fase 4 (los 3 flujos de `MainScreen`) y Fase 5.5 (`MainScreen`)
todavía no están migrados. La **Etapa A** junta lo que es seguro hacer ya (no depende de los
flujos); la **Etapa B** lo que conviene diferir hasta que MainScreen + flujos estén migrados
(borrar `fechas.js` plural está bloqueado por `FlujoCorte`; auditar WCAG/hover sobre superficies
aún sin migrar sería trabajo repetido).

Diagnóstico verificado al abrir la fase (2026-05-29): **cero clases utility de Tailwind** quedan
en `frontend/src/` — los `className=` que hay son todos clases scoped propias (`om-*-fila`,
`om-shake`, la dinámica de `DataTable`). MainScreen y los flujos ya usan inline styles. Por eso
Tailwind es removible ya, sin depender de migrar los flujos. `formatos.js` plural tiene **0 imports**;
`fechas.js` plural lo importan 4 archivos: `SelectorMes/Dia/Semana` (migrables ya) + `FlujoCorte` (bloquea).

**Etapa A — ahora (sin dependencias):** ✅ (2026-05-29)
- [x] Eliminar `frontend/src/utils/formatos.js` (plural) — 0 imports, confirmado. **Hecho.**
- [x] Migrar los 3 `Selector*` (`SelectorMes/Dia/Semana`) de `utils/fechas` → `utils/fecha` (singular). Solo cambió el path; el `fecha.js` consolidado ya exportaba todo lo necesario. **Hecho.**
- [x] Desinstalar Tailwind: quitado `@import "tailwindcss"` de `index.css`, borrados `tailwind.config.js` y `postcss.config.js`, sacadas `tailwindcss`/`@tailwindcss/postcss`/`autoprefixer`/`postcss` de `package.json`, `npm install` (removió 15 paquetes). **Build verificado OK** (CSS 1.62 kB sin el preflight de Tailwind). **Hecho.**
- [x] Resolver vulnerabilidades de `npm audit` (`deudas_tecnicas_frontend.md` #9): `npm audit fix` resolvió las 5 con fix (`vite`, `postcss`, `picomatch`, `brace-expansion`, `flatted`); build reverificado OK. **Queda `xlsx`** (sin fix oficial) — **decisión pendiente**: migrar a `exceljs` o aceptar el riesgo.

**Etapa B — tras migrar MainScreen + flujos (Fase 4 / 5.5):**
- [x] Eliminar `frontend/src/utils/fechas.js` (plural) — **Hecho (2026-05-29)**: `FlujoCorte` (último importador) migrado a `utils/fecha` (singular); archivo borrado. Cierra la parte "convivencia de archivos" de la deuda #8.
- [x] Resolver consolidaciones pendientes de §2 — **Hecho (2026-05-29)**. Ver §2.1: dentro de `frontend` no había colisiones reales (`fmtHora`/`fmtFechaCorta` del turnero no existen acá; son otro repo → D9, no deuda). Única redundancia interna: `formatARS` (0 usos, opción `{prefijo}` muerta) colapsado en `fmtPesos` (canónico, implementación inline). Comentarios stale de `formato.js`/`fecha.js` limpiados.
- [ ] Auditoría WCAG (`deudas_tecnicas_frontend.md` #6).
- [ ] Revisitar hover de tablas densas: ¿`:hover` scoped reutilizable? Medir (`deudas_tecnicas_frontend.md` #4/#21).

---

## 2. Consolidaciones pendientes (cerrar en Fase 6)

Cosas que durante Fase 1 quedaron a "mantener ambas" para no romper, y que hay
que unificar al final con un solo nombre / una sola implementación.

### 2.1 Utils de formato / fecha — colisiones a resolver

> **Cerrado (2026-05-29).** La tabla se escribió asumiendo una fusión de utils con
> `frontend-turnero`, pero son repos distintos: las "funciones turnero" (`fmtHora`,
> `fmtFechaCorta`) **no existen dentro de `frontend`** → no había colisiones reales.
> Per D9 la divergencia de API entre fronts es intencional (no deuda). La única
> redundancia interna (`formatARS` vs `fmtPesos`) se resolvió colapsando en `fmtPesos`.

| Función admin actual | Función turnero | Decisión tomada | Estado |
|---|---|---|---|
| `fmtPesos(n)` (`utils/formato.js`) | `fmtPesos(n)` | Único nombre `fmtPesos` con implementación inline. `formatARS` (alias con opción `{prefijo}` muerta, 0 usos) eliminado. | ✅ Resuelto |
| `formatHora(iso)` con TZ fija `America/Argentina/Buenos_Aires` | `fmtHora(iso)` con TZ del navegador | `fmtHora` no existe en `frontend`. Divergencia cross-front intencional (D9). | ✅ N/A (D9) |
| `formatFechaCorta(fechaStr)` → "Domingo 15/03" | `fmtFechaCorta(fechaStr)` → "lun 19/05" | `fmtFechaCorta` no existe en `frontend`. Divergencia cross-front intencional (D9). | ✅ N/A (D9) |
| `formatPago(forma)` | (no existe en turnero) | Mantener en admin. | OK |
| `getFechaHoy`, `getMesActual`, `getSemanaActual` | (no existen en turnero) | Mantener en admin. | OK |
| `desplazarMes`, `desplazarSemana`, `desplazarDia` | (no existen en turnero) | Mantener en admin. | OK |
| `mesALabel`, `semanaALabel`, `fechaALabel`, `semanaAFechaLunes`, `getISOWeekNum`, `semanaALunes`, `MESES`, `DIAS`, `TZ` | (no existen) | Mantener en admin. | OK |
| (no existe en admin) | `generarProximosDias`, `fmtFechaLarga`, `fmtFechaHora`, `diaSemana`, `diaNumero`, `nombreMes`, `esHoy`, `diaDeSemana` | Sumar al `fecha.js` consolidado del admin si Fase 4 los necesita. | A demanda |

### 2.2 Tailwind → inline-styles

Al final de Fase 4 no debería quedar ningún `className=` con utilities de Tailwind
en `frontend/src/`. Si queda alguno, listarlo acá antes de desinstalar.

- [x] Grep `className=` en `frontend/src/` (2026-05-29): **limpio**. Los únicos 12 usos son clases scoped propias (`om-*-fila`, `om-shake`, la dinámica de `DataTable`), no utilities de Tailwind. MainScreen y los flujos ya usan inline styles. Tailwind desinstalado en Etapa A.

---

## 3. Deudas técnicas detectadas durante este rediseño

> **Movidas a su propio registro.** Las deudas técnicas detectadas durante el rediseño
> (con su texto completo, plan de resolución escalonado por etapas, leyenda de estados,
> tabla-resumen y apéndice de resueltas) viven ahora en:
>
> **→ `docs/deudas_tecnicas_frontend.md`**
>
> Se conservan los números originales (#NN) para no romper referencias cruzadas en
> commits, chats y otros docs. Cuando aparezca una deuda nueva durante una tarea de este
> plan, anotarla directamente en ese registro (no acá).

<!-- Contenido histórico migrado a docs/deudas_tecnicas_frontend.md el 2026-05-29.
1. `frontend/src/App.css` — restos del scaffolding de Vite (`.logo`, `.card`, `.read-the-docs`) y un `@keyframes logo-spin` decorativo. Sospecha: nadie lo importa. Verificar y borrar (Fase 1).
2. `frontend/src/App.jsx` — `stylesEstado` hardcodea verde `#1a7a4a` y `DM Sans`. Incoherente con el theme. Se va en Fase 3.
3. `frontend/index.html` — `lang="en"` (deuda global del sistema de diseño #11) y carga **Raleway** (no Geist). Se resuelve en Fase 1.
4. Deuda heredada del sistema de diseño **#8** (inline-styles + useState para hover) — este front es el que más riesgo tiene por las tablas densas (Caja, Planillas, Balances, Ventas, Gastos). Re-evaluar al terminar Fase 4 con datos reales: si se siente lento, refactor puntual a `:hover` scoped via `<style>`.
5. Deuda heredada del sistema de diseño **#12** (focus visible global) — re-auditar primitivos al final de Fase 2.
6. Deuda heredada del sistema de diseño **#9** (contraste WCAG no verificado) — auditar en Fase 6.
7. `PantallaCargando` y `PantallaError` (en `App.jsx`) son ad-hoc y duplican la responsabilidad de los primitivos `Skeleton` y `EmptyState`. Se eliminan en Fase 3.
8. Convivencia temporal de `utils/formato.js` (nuevo, singular) y `utils/formatos.js` (viejo, plural). Misma situación con `fecha.js` y `fechas.js`. **No es deuda permanente** — se cierra en Fase 6, pero hay riesgo de imports cruzados si no se controla. Mientras dure: cuando se toque un archivo, migrar sus imports al singular nuevo.
10. **Doble fetch de `getNegocio`**: `App.jsx` lo llama para `logoUrl`/`bookingUrl`/`document.title`, y `PanelAdmin.jsx` lo llama de nuevo para `nombre_negocio`. El backend devuelve todo en una sola request. Pasar `nombreNegocio` por prop desde `App.jsx` o subir un context. Atacar durante Fase 3 al refactorizar PanelAdmin.
11. **`onPointerDown` usado en `MainScreen.jsx` y `PanelAdmin.jsx`** — prohibido por sistema de diseño §4.3 (rompe accesibilidad por teclado, no se puede cancelar deslizando). Reemplazar por `onClick` en Fase 3 (PanelAdmin) y Fase 5.5 (MainScreen). **Resuelto en PanelAdmin (Fase 3) y en los dos logins (Fase 4)**; queda MainScreen para 5.5.
12. **Campo `tenant.logo` legacy** — el backend todavía expone `logo` en `GET /negocio`. El front ya no lo lee (el logo viene de `tenant_imagen` tipo='logo'). Limpieza del backend pendiente para el merge de feature/turnero a main (anotada por el usuario, posiblemente ya esté en `estado_actual.md`).
13. ~~**`Field` divergente entre fronts**~~ — **Resuelto vía D9**: la extensión de API en un front sin reflejar en el otro ya no se considera deuda. Si turnero necesita las mismas props (`disabled`, `autoComplete`, etc.), se readaptan ahí cuando aparezca el caso.
14. **`LogoCirculo` duplicado**: el helper que muestra el logo del tenant dentro de un círculo (con fallback a icono `Lock`) está implementado dos veces, una en `PantallaLoginAdmin.jsx` y otra en `PantallaLoginOperativo.jsx`. Dos usos = momento de promover a `components/ui/` per §7.1, pero hay matiz: la versión admin acepta `lockColor` para reflejar el estado del PIN. **Plan**: promover a primitivo cuando MainScreen necesite el mismo patrón en Fase 5.5 (tercer caso). Mientras tanto, mantenerlas sincronizadas si se modifica alguna. Implementación actual: `object-fit: cover` para que el logo llene el círculo (caveat: logos no-cuadrados pueden recortarse — iterar si aparece en producción).
15. ~~**Excepción a regla "Skeleton no spinner"**~~ — **Resuelto vía D6**: la excepción dejó de ser puntual de `PantallaCargando` y se formalizó como regla del admin con el primitivo `LoadingState`. Anotación en `docs/sistema_de_disenio.md` §4.5 actualizada en este mismo ciclo.
16. ~~**`BadgeVariacion` candidato a primitivo**~~ — **Resuelto en chat de Balances**: promovido a `components/ui/BadgeVariacion.jsx` (segundo uso real en el tab Histórico, columna "vs anterior"). Versión idéntica a la anterior local de `SeccionInicio` — `SeccionInicio` migrada al primitivo (función local eliminada). Usos actuales: `SeccionInicio` (comparativo del mes), `SeccionBalances` (variación vs mes anterior en histórico).
17. **`EmptyState` no acepta `tone`** *(mejora opcional, no deuda contra otros fronts — D9)*: el wrapper del glyph fija `color: theme.muted`, por lo que cuando se usa con `IconoAlerta` (estado de error en `SeccionInicio`/`SeccionCaja`), el ícono se ve gris en lugar de rojo. **Plan**: agregar prop `tone` (`'muted' | 'success' | 'danger' | 'warning'`) al primitivo `EmptyState` del admin que pinte el color del wrapper del glyph. Atacar cuando una pantalla lo pida fuerte o como parte de un pase de QA de la fase.
18. **Tabla ad-hoc con `role="table"/"row"/"cell"` en `SeccionInicio`**: parche de accesibilidad mientras `DataTable` no exista. Se reemplaza naturalmente cuando se construya el primitivo en Fase 5 (esperado durante Ventas/Balances).
20. ~~**`DataTable` postergado a Fase 5**~~ — **Resuelto en chat A de Gestión**: construido en `components/ui/DataTable.jsx` al aparecer el primer caso CRUD real con sort + paginación + N filas (Servicios + Productos). API: `columns` (con `sortable`/`align`/`grow`/`render`/`sortAccessor`), `rows`, `rowKey`, `onRowClick`, `pageSize`. Las tablas de reporte (Balances/Caja/Ventas/Gastos/Planillas) **siguen ad-hoc** — no se migran porque no comparten requisitos (sin sort/paginación, con totales/subtotales y semánticas propias). Ver D14 (densidad distinta entre tablas de reporte y CRUD táctiles).
21. **Hover de fila vía `:hover` scoped en `SeccionCaja` y `SeccionVentas`**: para las tablas densas se usó un `<style>` inline con clase única (`om-caja-fila:hover`, `om-ventas-fila:hover`) en vez del patrón `useState` por fila, porque éste último explota re-renders al pasar el mouse en tablas con N filas. Excepción consciente al patrón general (§4.2). Si la deuda #8 se ataca con `:hover` scoped vía `<style>` reutilizable, este caso se absorbe.
22. ~~**Inconsistencia visual del botón eliminar entre Caja y Ventas/Gastos**~~ — **Resuelta** al promover `BotonIconoFila` a primitivo y migrar el botón de Caja al mismo patrón con caja (chat de Gastos).
23. **Sub-componentes locales candidatos a primitivos** (estado actualizado tras chat de Gastos):
    - ~~`BotonIconoFila`~~ — **promovido** a `components/ui/BotonIconoFila.jsx`. Usos: Ventas, Gastos, Caja.
    - ~~Shell visual del modal de edición~~ — **promovido** como primitivo `Modal`. Usos: Ventas (`ModalEditarVenta`), Gastos (`ModalEditarGasto`).
    - ~~`SelectFormaPago`~~ — **promovido** como primitivo genérico `Select`. Usos: Ventas, Gastos (incluye categoría dinámica).
    - ~~`DetalleVentaConfirm` / `DetalleMovimientoConfirm`~~ — **promovido** como primitivo `DetalleRecurso`. Los callers arman el array de filas. Usos: Ventas, Gastos, Caja.
    - `CampoFijo` — sigue local en Ventas (único uso). Promover si aparece en otra sección.
24. **`ConfirmDialog` no usa internamente el primitivo `Modal`**: tienen shells visuales casi idénticos (overlay + card + animaciones + ESC). El refactor de `ConfirmDialog` para reusar `Modal` quedó fuera de scope del chat de Gastos (`ConfirmDialog` viene heredado idéntico de turnero y refactorizarlo significa rotar también el turnero, o introducir divergencia). Plan: evaluar en Fase 6 (cleanup) — si quedan ambos, está OK (D9: APIs pueden divergir), pero hay duplicación visual que conviene unificar.
25. **`Modal` primitivo usa id estático `om-modal-title` para `aria-labelledby`**: si dos modales del primitivo se abrieran simultáneamente, los ids colisionarían. Hoy nunca pasa (los flujos del admin son monomodal). Si en algún futuro se anidan modales o aparecen modales superpuestos, generar id único con `useId()`.
26. ~~**`ChipBarbero` candidato a primitivo (chip de filtro)**~~ — **Resuelto en chat de Turnero**: promovido a `components/ui/ChipFiltro.jsx` (segundo uso real: filtro de barberos en SeccionTurnero). API extendida con prop `size: 'md' | 'sm'` para soportar jerarquías diferenciadas dentro de la misma sección (`md` para filtros primarios, `sm` para secundarios con padding más ajustado y weight fijo `medium`). `SeccionPlanillas` migrada al primitivo (definición local eliminada). Usos actuales: Planillas (filtro de barbero), Turnero (filtro de barbero `md` + filtro de estado `sm`).
27. **Semántica engañosa de la columna comisión del subtotal del día (`SeccionPlanillas`, tab Detalle)**: el `thead` no tiene columna "Comisión"; el slot de "Pago" del `tfoot` muestra el valor `comisión + propinas` sin label explícito (solo el eyebrow mono al lado indica `XX% + prop` o `$N/c + prop`). El usuario pidió explícitamente mantener la semántica actual durante el rediseño (opción "solo cambiar tokens"). Vale revisitar como mejora UX en pase de QA de Fase 6 o cuando aparezca un pedido de usuario — opciones evaluadas en el chat: (a) fila aparte debajo del subtotal con "Comisión del día" + "A pagar al barbero", (b) sumar columna "Comisión" al `thead` con valor por corte.
28. **Acoplamiento sutil entre tabs en `SeccionPlanillas`**: `infoComisionActiva` se deriva de `resumenData` pero se consume en el tab Detalle (para calcular la comisión por día). El `Promise.all` que carga ambos endpoints en paralelo lo garantiza hoy. Si en el futuro se separan los fetches (ej. lazy load por tab) o se mueve el cálculo de comisión al backend, este acoplamiento se rompe silenciosamente — el render del Detalle se quedaría sin comisión hasta que cargue el Resumen. Anotar para no perderlo si se refactoriza la carga.
29. ~~**Bug latente en `SeccionBalances`** (línea 370 del original)~~: `<p>Sin movimientos en {labelMes(mesSeleccionado)}.</p>` referenciaba la función `labelMes`, que **no estaba definida ni importada** en el archivo. `ReferenceError` latente, se gatillaba en el caso vacío (mes sin barberos ni gastos). **Resuelto** en el rediseño — reemplazado por `mesALabel(mesSeleccionado)` (función real de `utils/fecha.js`) dentro de un `EmptyState`.
30. ~~**Primitivo `Toast` pendiente**~~ — **Resuelto en chat A de Gestión** (parte auto-dismiss): construido `components/ui/Toast.jsx` con variantes `success | danger | warning | info`, prop `autoDismissMs` (timer interno → `onDismiss`) y `dismissible` (botón X). `role="status"` + `aria-live="polite"`. **No** es flotante global — el caller lo renderiza en el flow donde quiere (decisión: el posicionamiento sticky global se evalúa si aparece la necesidad). Migrados: `BannerError` de `SeccionTurnero` (danger, 6s, dismissible) y `mensajeExito` de `TabSeguridad` (success, 4s). **Matiz**: el feedback inline **persistente** (sin auto-dismiss) de `TabTurnero` (`MensajeFeedback` local — aparece al guardar, se limpia al editar) NO usa Toast: es un patrón distinto (mensaje en form, no efímero). Si aparece un 2do caso de ese patrón, promover aparte.
33. **`Number()` sin validar NaN en inputs de precio/stock** (`TabServicios`, `TabProductos`): el guardado castea con `Number(valor)`. Hoy mitigado por el filtro `\D` (los inputs solo aceptan dígitos), pero si el backend recibiera el body por otra vía no habría guard. Bajo riesgo. Revisitar si se agrega validación de formularios formal.
34. **`/admin/productos/:id/agregar-stock` no es transaccional con el PUT del producto** (`TabProductos`): el guardado hace PUT producto y luego PUT agregar-stock como dos requests. Si falla el segundo, el primero ya quedó persistido (queda el producto guardado pero sin el stock sumado). Es backend — evaluar endpoint único que reciba producto + delta de stock, o transacción. Anotado para chat backend.
35. **Cambio de credenciales operativas sin re-autenticación** (`TabSeguridad`): cambiar usuario o contraseña del modo operativo no pide la contraseña actual (a diferencia del PIN admin, que sí pide el PIN actual). El flujo asume sesión admin ya autenticada (protegida por PIN admin), pero alguien con acceso físico al panel abierto podría cambiar credenciales operativas sin reto. Evaluar pedir contraseña actual / re-validar PIN admin antes de estos cambios. Deuda de seguridad, no visual.
36. **Carga del usuario operativo sin reintento** (`TabSeguridad`): si `getCredencialesOperativas()` falla, `usuarioOperativo` queda `null` y la fila muestra el pill "Sin configurar" — semánticamente erróneo (no es que no esté configurado, es que no se pudo cargar). Distinguir el estado de error del estado "vacío real" (ej. flag `errorCarga` que muestre un Toast/Reintentar en lugar del pill).
37. ~~**`InputTiempo` local en `TabBarberos` — candidato a primitivo**~~ — **Resuelto en chat de `TabNegocio`**: promovido a `components/ui/InputTiempo.jsx`, generalizado a `time`/`datetime-local`/`date` + props `min`/`max`/`step`/`disabled`. Usos: `TabBarberos`, `BloqueHorarioAtencion`, `BloqueFeriados`. `TabBarberos` migrado (definición local eliminada).
38. **`Modal` primitivo sin scroll interno**: la card del `Modal` (`components/ui/Modal.jsx`) no acota su altura ni hace scroll — si el contenido supera el viewport, se sale de pantalla (overlay con `padding:16` pero sin `maxHeight`/`overflow`). Esto disparó el pedido de "modal horizontal" del sub-panel Horario (7 días apilados desbordaban). Se mitigó allá con grilla horizontal + `maxWidth:900`, pero la deuda del primitivo sigue. **Fix**: `maxHeight: calc(100vh - 32px)` + `overflowY:auto` en la card (idealmente header/footer fijos y solo `children` scrollea — requiere reestructurar el layout interno del `Modal` en 3 zonas).
39. **`humanizarDiasEnMensaje` acoplado al wording del backend** (`TabBarberos`): el mensaje de solapamiento del backend se traduce client-side con un regex sobre `"día N"` → nombre del día. Si el backend cambia el wording (idioma, formato), el regex deja de matchear y el mensaje se muestra crudo (no rompe, solo degrada). Alternativa robusta: validar el solapamiento en el cliente **antes** del PUT y construir el mensaje localmente (duplica la lógica de validación que ya vive en el backend). Bajo riesgo — se aceptó la fragilidad a cambio de no duplicar reglas.
40. **Sin verdad-cliente del rango horario del local en el editor de horarios del barbero** (`TabBarberos`): se bloquea el **día** cerrado del local (card atenuada, sin "Agregar bloque"), pero en un día abierto el editor deja cargar **cualquier** rango horario (ej. 08:00–22:00 aunque el local atienda 10:00–19:00). El backend lo rechaza (y el mensaje se humaniza, ver #39), pero la UI no lo previene. **Mejora**: acotar el `InputTiempo` al rango del local de ese día (min/max) o avisar inline antes de guardar. Requiere tener el rango del local cargado (ya lo tenemos: `horarioLocal[dia] = {inicio, fin}`).

41. **Wrapper "label + `InputTiempo`" duplicado — candidato a primitivo** (`CampoTiempo`): existe como sub-componente local en `TabBarberos` (eyebrow mono + `InputTiempo` full, para el form de ausencias) y, tras el chat de `TabNegocio`, también inline en el modal de alta de `BloqueFeriados` (label "Fecha" + `InputTiempo type="date"`). 2 usos del mismo patrón. Si aparece un 3er uso (§7.1), promover un `CampoTiempo` a `components/ui/` — o, alternativa más limpia, sumar una prop `label` opcional al propio `InputTiempo` para que se autoetiquete como `Field`. Bajo riesgo; sin promover por ahora.

42. **`BloqueFeriados` con `overflow` propio** (`max-height:260` + `overflowY:auto` en la lista): es una **divergencia consciente** de la convención "las secciones no tienen overflow propio" (convenciones §Scroll global del panel). Justificada porque la lista de feriados es de largo variable y vive dentro del bloque sin scroll de `TabNegocio` (D16). Si en el futuro se decide que el panel de Gestión scrollee normalmente, revisar si este scroll interno sigue teniendo sentido.

43. **Nombre del negocio + URL de reservas sin self-service** (`TabNegocio`): el chat de `TabNegocio` removió el formulario de esos dos campos (decisión del usuario: se editan directo en la DB porque cambian muy poco). El endpoint `GET/PUT /admin/negocio` sigue existiendo y `App.jsx`/`PanelAdmin` siguen leyendo el nombre/logo. **Nota de producto**: si en algún momento el producto se vende a terceros (dueños que quieran renombrar su negocio o cambiar la plataforma de turnos sin tocar la DB), reponer un formulario mínimo. Hoy no es necesario.

31. **Endpoint admin no expone `horario_atencion` global del tenant** — `getNegocio()` devuelve `nombre_negocio`, `booking_url`, `logo` pero no las horas de operación. La agenda de `SeccionTurnero` deriva el rango del propio dataset de turnos (min/max con padding 1h, clamp a 08:00–22:00). Si no hay turnos, fallback puro 08:00–22:00. Para que la agenda muestre la jornada real aun sin turnos cargados (o aun en días flojos), el backend debería exponer `horario_atencion` global del tenant — probable extensión a `getNegocio()` o endpoint nuevo `/admin/horario-atencion`. Cuando esté, reemplazar el cálculo local del rango por ese valor. **Actualización (chat B Gestión)**: el endpoint `GET/PUT /admin/horario-atencion` **ya existe** — devuelve el horario semanal por día (`[{dia_semana, abierto, hora_inicio, hora_fin}]`, días cerrados con horas `null`). Lo consumen `BloqueHorarioAtencion` (CRUD) y ahora `TabBarberos` (defaults de bloque + bloqueo de días cerrados). `SeccionTurnero` podría reemplazar su cálculo min/max de turnos por este valor para mostrar la jornada real aun sin turnos cargados.

32. **Endpoint `/admin/turnos` no devuelve `forma_pago`** — para turnos `completado`, el dueño querría ver en qué se pagó (efectivo / MP / etc.) desde el modal de detalle. Hoy el endpoint no lo trae (el campo vive en `pago` o `corte`, no en `turno`). Cuando se extienda el endpoint, sumar la fila al modal y promover `BadgeFormaPago` a primitivo (sería el 5to uso confirmado: Caja, Ventas, Gastos, Planillas, Turnero).

9. **Vulnerabilidades reportadas por `npm audit`** (detectadas al instalar `lucide-react` en Fase 1 — no las introdujo lucide, son preexistentes en el árbol de dependencias):
   - **Con fix disponible** (resolver con `npm audit fix`): `vite` (3 advisories, alta), `postcss` (XSS, moderada), `picomatch` (ReDoS + injection, alta), `brace-expansion` (DoS, moderada), `flatted` (prototype pollution, alta).
   - **Sin fix oficial**: `xlsx` (prototype pollution + ReDoS, alta). SheetJS no publica fix en npm. Evaluar migrar a `exceljs` o aceptar el riesgo documentado.
   - Tratamiento: resolver al final de todo (Fase 6) para no mezclar scope con el rediseño visual.
-->

---

*Última actualización: 2026-05-26 — `SeccionCaja` cerrada. Decisiones nuevas: D8 (variante del primitivo `Tabs` = underline) y D9 (política de divergencia entre fronts: solo los estilos estéticos se mantienen consistentes, las APIs pueden divergir sin ser deuda — documentada en `docs/sistema_de_disenio.md` §8). Primitivo nuevo: `Tabs`. Primitivo extendido: `ConfirmDialog` con `children` opcional. Sub-componentes locales (`BadgeFormaPago`, `TogglePill`, `BotonExportarExcel`) rediseñados in-place; promoverlos a `/ui/` queda en Fase 5. Deudas nuevas: #20 (`DataTable` postergado), #21 (`:hover` scoped en tabla de Caja). Deudas resueltas vía D9: #13 (`Field` divergente). Deuda #17 reformulada como mejora opcional (sin parte "backport"). Próximo chat: `SeccionVentas`.*

*Última actualización: 2026-05-27 — `SeccionPlanillas` cerrada. Sin primitivos nuevos en `components/ui/`. Sub-componente local nuevo: `ChipBarbero` (chip de filtro — primer caso del patrón en el admin; promover cuando aparezca un segundo uso). Decisión visual de la sección: barberos como **chips de filtro** (no como segunda barra de Tabs underline), por ser un filtro y no una navegación entre vistas. `BadgeFormaPago` confirma 4to uso (Caja, Ventas, Gastos, Planillas) — sigue local pendiente de promoción en Fase 5. Deudas nuevas: #26 (`ChipBarbero` candidato a primitivo), #27 (semántica engañosa de la columna comisión en subtotal del día), #28 (acoplamiento sutil entre tabs vía `infoComisionActiva` derivada de resumen). Próximo chat: `SeccionBalances`.*

*Última actualización: 2026-05-27 — `SeccionTurnero` rediseñada (no 1:1 — se replanteó UX a fondo). **Primitivo nuevo**: `ChipFiltro` promovido desde `ChipBarbero` local de Planillas (segundo uso real). API extendida con prop `size: 'md' | 'sm'` para soportar jerarquías diferenciadas dentro de la misma sección. `SeccionPlanillas` migrada. **Decisiones nuevas**: **D11** (`SeccionTurnero` sin `ScreenHeader`, excepción consciente — única sección sin header para maximizar verticalidad de la agenda) y **D12** (agenda construida in-house con CSS grid + position absolute, sin librería de calendar — evaluado y descartado `react-big-calendar` / `FullCalendar` por peso y overrides agresivos vs caso acotado). Estructura: doble vista (Agenda / Lista) con toggle, KPIs del día (4 mini-cards), dos niveles de chips de filtro (barberos primarios, estado secundarios), línea "ahora" indigo en la agenda cuando es hoy (refresh 60s), modal de detalle único con copiar al portapapeles para teléfono/email del cliente. Modal reemplaza al ad-hoc anterior y unifica detalle + acción + confirmación de cancelación sin anidar modales. `alert()` reemplazado por sub-componente local `BannerError` con auto-dismiss 6s. Deudas resueltas: **#26** (`ChipFiltro` promovido). Deudas nuevas: **#30** (primitivo `Toast` pendiente — banner inline es parche temporal), **#31** (endpoint admin debe exponer `horario_atencion` global del tenant para que la agenda muestre la jornada real sin turnos cargados), **#32** (endpoint `/admin/turnos` debe devolver `forma_pago` para turnos completado — desbloquearía 5to uso de `BadgeFormaPago`). Próximo chat: `SeccionGestion`.*

*Última actualización: 2026-05-28 — `TabBarberos` rediseñada (chat B de Gestión, primer tab de `SeccionGestion`). Sin primitivos nuevos en `components/ui/`. **Arquitectura Opción C**: lista en `DataTable` (3er uso real, cierra el "reuso esperado" de la deuda #20) + click en fila → `ModalDatosBarbero` (datos básicos: nombre, PIN, comisión entera 0–100, estado) + acción de fila `CalendarClock` → `ModalAgenda` ancho (`maxWidth:900`) con `Tabs` internos Horario / Ausencias (montados a la vez vía toggle de `display`, sin refetch al cambiar de tab). Columna nueva **Horarios** (check `Check` verde centrado por flex si el barbero tiene bloques). **Precarga** de horarios de todos los barberos en el mount con `Promise.allSettled` (alimenta el check + cachea para abrir el modal sin refetch; un fallo puntual deja ese barbero en "—" sin romper la tabla). Sub-panel Horario en **grilla horizontal** (`repeat(auto-fill, minmax(260px,1fr))`) porque el stack vertical de 7 días desbordaba el `Modal` (disparó deuda #38); "Agregar bloque" arranca con el **horario del local** de ese día (vía `GET /admin/horario-atencion`, fallback 09:00–18:00); **días cerrados del local bloqueados** (card `surfaceAlt`, "Local cerrado", sin botón de agregar) usando `horarioLocal` con sentinel `null` = "desconocido" para degradar seguro ante fallo de red; mensaje de solapamiento del backend **humanizado** (`"día 5"` → `"Viernes"` vía `humanizarDiasEnMensaje`). Sub-panel "Suspensiones" renombrado a **"Ausencias"**. Botones Guardar / Cerrar alineados a la derecha. `InputTiempo` vive local (candidato a primitivo). Deudas nuevas: **#37** (`InputTiempo` → primitivo, 2do uso en `BloqueHorarioAtencion`), **#38** (`Modal` sin scroll interno), **#39** (`humanizarDiasEnMensaje` acoplado al wording del backend), **#40** (sin verdad-cliente del rango horario del local — bloquea el día cerrado pero no acota el rango en día abierto). Nota agregada a **#31**: `/admin/horario-atencion` ya existe (GET/PUT). Próximo chat: `TabNegocio` (+ `BloqueHorarioAtencion`, `BloqueFeriados`, `BloqueImagenes`; renombrar la tab "Datos del negocio"; promover `InputTiempo`).*

*Última actualización: 2026-05-27 — `SeccionBalances` cerrada. **Primitivo nuevo**: `BadgeVariacion` promovido desde `SeccionInicio` (segundo uso real en el tab Histórico). `SeccionInicio` migrada al primitivo (eliminada función local). Decisión visual de la sección: **balance neto positivo en `theme.accent`, negativo en `theme.danger`** (opción b del chat — conserva la señal del signo dentro del lenguaje del sistema, sin verde). KPI Ingresos siempre `accent`, KPI Egresos siempre `danger`. Pill "ACTUAL" del mes vigente en histórico = misma forma que pill "HOY" de Planillas. Deudas evaluadas y **postergadas**: #20 (`DataTable` — la complejidad de las 3 tablas de Balances sigue siendo la misma que Caja/Ventas/Gastos; espera real es `SeccionGestion`/`SeccionTurnero`), #26 (`ChipFiltro` — Balances no tiene filtros multi-opción). Charts en histórico: no se sumaron (rediseño 1:1 con el original); queda como conversación aparte si se decide librería. Deudas resueltas: **#16** (`BadgeVariacion` promovido), **#29** (bug latente `labelMes` no definida en el caso vacío — fixeado con `mesALabel`). **Decisión nueva D10**: jerarquía visual de tablas densas del admin — sacar bandas grises (`surfaceAlt`) de thead, header de bloque y fila total; anclar el total con `border-top: 2px solid theme.accent` (línea indigo). Disparada por la pregunta del usuario sobre el contraste de los títulos de columna con el fondo. Patrón **propagado en este mismo chat** a las 5 secciones ya migradas: Balances, Caja (solo thead — no tiene fila total), Ventas (TablaMes + TablaResumen), Gastos (TablaGastos + TablaResumen), Planillas (ThCelda compartido + tfoot subtotal-día + tfoot TOTAL del resumen; header del bloque-día pasa de `surfaceAlt` a `surface` blanco con border-bottom, el día actual conserva `accentSoft` como excepción consciente). Próximo chat: `SeccionTurnero`.*

*Última actualización: 2026-05-28 — `SeccionGestion` **chat A** cerrado: shell + `TabServicios` + `TabProductos` + `TabTurnero` + `TabSeguridad`. **4 primitivos nuevos**: `DataTable` (sort + paginación + `onRowClick` + `col.grow`; cierra **deuda #20**), `BadgeEstado` + `ToggleEstado` (promovidos al 2do uso Servicios→Productos), `Toast` (feedback efímero auto-dismiss + variantes; cierra **deuda #30** parte auto-dismiss — migrados `BannerError` de Turnero y `mensajeExito` de Seguridad). **Primitivo extendido**: `Field` + prop `inputMode` (todos los inputs numéricos pasan a `type=text inputMode=numeric` + filtro `\D` → numpad iPad, sin "e", placeholder limpio). **Decisiones nuevas**: **D13** (Gestión SÍ usa `ScreenHeader`; el usuario evalúa a futuro eliminar/repensar los headers en general — anotado, no decidido), **D14** (densidad de fila distinta: tablas de reporte `hairlineSoft` compactas vs `DataTable` CRUD táctil ~46–48px con `hairline`, por uso en iPad). **Cambio UX `TabProductos`**: creación pide "Stock inicial" (no "Stock mínimo"). Shell migrado de segmented-pill con emojis a `Tabs` underline + íconos Lucide (primer caso con 6 tabs). Click en fila de Servicios/Productos → modal de edición (pedido del usuario). Deudas nuevas: **#33** (`Number()` sin validar NaN — bajo riesgo), **#34** (agregar-stock no transaccional — backend), **#35** (cambio de credenciales operativas sin re-autenticación — seguridad), **#36** (carga de usuario operativo sin distinguir error de vacío). **Chat B pendiente**: `TabBarberos` + `TabNegocio` (+ bloques Horario/Feriados/Imágenes) — **y proponer nuevo nombre para la tab "Datos del negocio"**.*

*Última actualización: 2026-05-29 — **Fase 4 COMPLETA**: rediseñados los 3 flujos operativos (`FlujoCorte`/`FlujoVenta`/`FlujoGasto`). **Decisión de diseño**: contenido de cada paso en un **panel claro casi full-screen** centrado sobre el fondo del local (foto blur + velo), reusando el sistema tal cual adentro (opción B; se descartó "contenido flotando sobre el velo"). **Primitivo nuevo `FondoLocal`** (`components/ui/`) — encapsula imagen+velo+fallback+constantes; `MainScreen` refactorizado para usarlo (sin cambio visual), `App.jsx` pasa `imagenLocal` a los flujos. **Módulo compartido nuevo `flows/wizard.jsx`** (fuera de `components/ui/`, superficie kiosko-wizard como los wizard-only del turnero §6.2): `PanelWizard`, `BotonOpcion`, `CampoTexto`, `CampoMonto`, `FilaResumen`, `FilaTotal`, `PantallaExito`, `wizardStyles`. Transición entre pasos simplificada (cambio instantáneo + `om-fade` vía `key`, se sacó el slide de 3 fases con refs/timers). Eliminado de los 3 flujos: DM Sans, `clamp()`/vw-vh, verde `#1a7a4a` y grises hardcoded, emojis, `onPointerDown`, `<input type=number>`, `<p>` planos de carga/error. **Fase 6 Etapa B**: `utils/fechas.js` (plural) **eliminado** — `FlujoCorte` (último importador) migrado a `utils/fecha` (singular, `formatHora` TZ-fija idéntica). Deuda **#8** cerrada en su parte "convivencia de archivos" (queda aparte la unificación de nombres cross-front de §2.1). Cleanups de paso: guard `cancelado` en el fetch de turnos de `FlujoCorte`; `respuesta` sin usar en `FlujoVenta`. **Próximo**: Fase 6 Etapa B restante (consolidación de nombres §2.1 con turnero, auditoría WCAG #6, hover de tablas densas #4/#21).*

*Última actualización: 2026-05-29 — `TabNegocio` cerrada → **`SeccionGestion` COMPLETA** (las 6 tabs en el sistema de diseño). **Primitivo nuevo**: `InputTiempo` promovido desde `TabBarberos` a `components/ui/` (cierra **deuda #37**), genérico para `time`/`datetime-local`/`date` + `min`/`max`/`step`/`disabled`; migrados `TabBarberos`, `BloqueHorarioAtencion`, `BloqueFeriados`. **Rename**: tab "Datos del negocio" → **"Negocio"**. **Decisiones nuevas**: **D15** (sacar el `ScreenHeader` de `SeccionGestion` por pedido del usuario — revierte D13; Gestión queda 2ª sección sin header tras Turnero; pendiente abierto: sacar headers del resto del admin sería un pase global no decidido) y **D16** (layout de `TabNegocio` = un único bloque dividido — Horario franja full-width + Feriados \| Imágenes en columnas; se removió el formulario de nombre/URL, que ahora se gestionan por DB). **Bloques migrados** (card-less, dentro del bloque de `TabNegocio`): `BloqueHorarioAtencion` (grilla horizontal de días + toggle local `ToggleAbierto` + `InputTiempo`; modal de cascada 409 ad-hoc → `ConfirmDialog`+`DetalleRecurso`; lógica de `claveHorario`/validación/409 intacta; **shape del endpoint sin cambios** porque lo consume TabBarberos), `BloqueFeriados` (alta → `Modal`+`InputTiempo type="date"`; cascada/borrado → `ConfirmDialog`; lista con scroll interno `max-height:260`), `BloqueImagenes` (miniaturas + tile "+" por grupo en vez de los 8 cuadrados fijos; borrado → `ConfirmDialog`; compresión WebP intacta). Feedback unificado con `Toast` en los 3 bloques. Deudas resueltas: **#37** (`InputTiempo` promovido). Deudas nuevas: **#41** (wrapper "label + `InputTiempo`" duplicado — candidato a primitivo `CampoTiempo`), **#42** (`BloqueFeriados` con overflow propio — divergencia consciente de la convención de scroll), **#43** (nombre/URL del negocio sin self-service — nota de producto). **Próximo chat**: Fase 4 cierra con los 3 flujos de `MainScreen` (`FlujoCorte`/`FlujoVenta`/`FlujoGasto`) + Fase 5.5 (rediseño de `MainScreen`).*
